# UI talks to the AG-UI SSE endpoint.
#
# Recommended for local dev (avoids CORS): use Vite proxy.
# - UI calls:        VITE_API_URL=/api
# - Vite proxies to: VITE_BACKEND_URL=http://localhost:8123
# For humans/tools (not exposed to Vite by default)
API_URL=/api
BACKEND_URL=http://localhost:8123

# For Vite (must be prefixed with VITE_)
VITE_API_URL=/api
VITE_BACKEND_URL=http://localhost:8123

# Vite dev server port
VITE_DEV_PORT=5174
