# ====== LLM (Zhipu / BigModel) ======
LLM_PROVIDER=zhipu
ZHIPU_MODEL=glm-4.7
ZHIPU_BASE_URL=https://open.bigmodel.cn/api/paas/v4/
ZHIPU_API_KEY=
# Compatibility fallback (optional): some clients read OPENAI_API_KEY
OPENAI_API_KEY=

# ====== SQL benchmark DB ======
CHINOOK_DB_PATH=./data/Chinook.db

# ====== MCP chart tool (optional) ======
# Requires Node.js + npx. If not available, start will fail (no fallback).
ENABLE_MCP_CHART=0

# ====== Artifact download (priority: CSV via data URL) ======
# Agent will return `data:text/csv;base64,...` when user asks to download.
ENABLE_DOWNLOAD_TOOL=1
